---
kind: pipeline
type: docker
name: shellcheck

concurrency:
  limit: 1

steps:
  - name: shellcheck
    image: koalaman/shellcheck-alpine
    commands:
    - ./test/shellcheck.sh
      
trigger:
   branch:
   - master 
   - beta/*
   - staging/*
   - release/*
   event:
   - push
   
