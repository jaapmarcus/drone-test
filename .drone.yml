---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64
  
steps:
    - name: greeting
      commands:
      - echo "$DRONE_BRANCH" >> /root/test.txt
      - echo "$DRONE_REPO" >> /root/test.txt
