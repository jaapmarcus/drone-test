---
kind: pipeline
type: docker
name: shellcheck

concurrency:
  limit: 1

steps:
  - name: shellcheck
    image: koalaman/shellcheck
    commands:
    - ./test/shellcheck.sh
      
trigger:
   event: [ pull_request, push ] 
